#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	float n;
	char m;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n);
	}

	for (m = 61; m <= 66; m++)
	{
		putchar(m);
	}
	putchar('\n');

	return (0);
}
